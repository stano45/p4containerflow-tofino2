# Build outputs (compiled Go binaries at repo root, not cmd/ source dirs)
/server
/loadgen
/collector
/bin/

# Results (keep directory via .gitkeep)
results/*.csv
results/*.png
results/*.json

# Local config (copy config_hw.env.example to config_hw.env and edit)
config_hw.env

# Go build cache
/vendor/
