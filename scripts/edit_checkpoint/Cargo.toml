[package]
name = "edit_checkpoint"
version = "0.1.0"
edition = "2021"
description = "Edit CRIU checkpoint archive for migration (IP patch in files.img)"

[[bin]]
name = "edit_checkpoint"
path = "src/main.rs"

[dependencies]
tar = "0.4"
serde_json = "1.0"
tempfile = "3.10"
